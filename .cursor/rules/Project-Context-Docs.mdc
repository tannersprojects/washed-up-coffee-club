---
description: Use project docs for app context (overview, schema, compliance)
alwaysApply: true
---

# Project Context Documentation

When answering questions about this app, making architectural decisions, or implementing features, **reference these docs** for accurate context:

## Primary Docs

1. **`docs/project/project_overview.md`** — Start here for:
   - What the app is (running club leaderboard, Strava-only auth)
   - Tech stack (SvelteKit, Svelte 5, Supabase, Drizzle)
   - Architecture (colocation, smart objects, runes)
   - Main routes and Strava compliance constraints

2. **`docs/project/project_context.md`** — Use for:
   - Dense reference: schema summary, auth flow, webhooks
   - Database tables (profile, strava_connections, challenges, challenge_participants)
   - Admin workflow, file map, implementation details
   - Compliance rules (attribution, terminology, data privacy)

3. **`docs/project/file_structure_plan.md`** — Use for:
   - Directory structure (lib vs routes, _logic, _components)
   - State management tiers (global, context, local)
   - Colocation and smart object patterns
   - Architectural decisions when generating code

## When to Use

- **Onboarding / “what is this app?”** → Read `project_overview.md` first.
- **Schema, API, or implementation details** → Use `project_context.md`.
- **Directory structure, folder layout, or state patterns** → Use `file_structure_plan.md`.
- **Compliance (Strava, attribution, user limits)** → Both docs; see also `docs/strava_compliance.md` if needed.

Do not guess app purpose, schema, or constraints—pull from these files or suggest the user open them with `@docs/project/project_context.md`, `@docs/project/project_overview.md`, or `@docs/project/file_structure_plan.md`.
