# Supabase (from Project Settings → API in dashboard)
PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
PUBLIC_SUPABASE_PUBLISHABLE_KEY=your-anon-key
SUPABASE_SECRET_KEY=your-service-role-key

# Database (from Project Settings → Database → Connection string URI)
# Used by Drizzle and drizzle-kit (migrations, db:generate, etc.)
DATABASE_URL=postgresql://postgres.[ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres

# Strava API (from https://www.strava.com/settings/api)
STRAVA_CLIENT_ID=your-client-id
STRAVA_CLIENT_SECRET=your-client-secret
# Must match Authorization Callback Domain; use prod URL when deploying
STRAVA_REDIRECT_URI=https://yourdomain.com/auth/strava/callback
